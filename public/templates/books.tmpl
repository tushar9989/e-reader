<div class="current-view books list">
    {{range .Books}}
    <div class="book">
        <div class="meta">
            <a href="/static/reader/viewer.html?id={{.ID}}">{{.Name}}</a>
        </div>
    </div>
    {{end}}
</div>

{{if not .Books}} Not found (or still indexing){{end}}